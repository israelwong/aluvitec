---
interface Props{
    title?: string,
    description?: string,
}
const {title='no definido', description='no definido'} = Astro.props

---
<div>
    <h1>
        {title}
    </h1>
    <p>
        {description}
    </p>
</div>